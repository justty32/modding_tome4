# MAINTENANCE — 工作流維護

這套文件也需要整理。目標是保持可用，不是保留所有歷史。

## 週期

建議在重大變更後，或每月做一次快速檢查。

## 檢查清單

- `AGENTS.md` 是否仍然薄，只放 always-on 與入口。
- `WORKFLOWS.md` 是否能正確派發，沒有過時工作流。
- `PRINCIPLES.md` 是否仍反映實際工作方式。
- `SESSION-LOG.md` 是否只列 open/in-flight，完成項是否已移除。
- `WAIT_USER.md` 是否有過期等待項。
- CODE_MAP 是否仍能讓 agent 修改前定位檔案。
- `archive/` 是否混入現役內容。
- analysis/research 是否有出口，沒有變成資料墓地。
- HTML 導覽層若存在，是否仍有使用價值；落後太多就刪除或重生。

## 刪除規則

可以刪或合併的內容：

- 連續 30 天沒用、沒有 open 項、沒有 durable 知識的子工作流。
- 被新規則取代的舊規則。
- 完成後只剩歷史價值、但不影響現役工作的筆記。
- 空的 `gotchas.md`、`archive/`、`session-log.md`，若所在工作流不需要它們。

刪除前問兩個問題：

- 下一個 agent 還需要它完成現役工作嗎？
- 它是否比 git log/archive 更適合保存這段資訊？

若答案都是否定，刪掉或併回上層 README。

## CODE_MAP 維護

- 新增/刪除原始碼檔案時更新。
- 檔案職責明顯改變時更新。
- 測試搬家時更新 Tests 欄。
- 不因小型內部實作細節變動而更新。

## session-log 維護

- 只保留 open。
- 完成即移除。
- 超過 50 行就刪舊留新，或拆成工作流/主題檔。
- 不用它記完整歷史。

